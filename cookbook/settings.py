from .config.dev import *

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.mysql',
        'NAME': 'django-project',
        'USER': 'root',
        'PASSWORD': '998219',
    }
}
